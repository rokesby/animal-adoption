import { createContext } from "react";
// const Context = createContext(localStorage.getItem("token"));
const Context = createContext(); // I think we should be passing an argument here
export default Context;
